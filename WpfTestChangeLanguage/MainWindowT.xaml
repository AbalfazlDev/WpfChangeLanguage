<Window x:Class="WpfTestChangeLanguage.MainWindowT"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTestChangeLanguage"
        mc:Ignorable="d"
        Title="MainWindowT" Height="450" Width="800">
    <Grid>
        <Menu>
            <MenuItem Header="{Binding MenuDashboard}" />
            <MenuItem Header="{Binding MenuProducts}" />
            <MenuItem Header="Language">
                <MenuItem Header="English" Command="{Binding ChangeLanguageCommand}" CommandParameter="en" />
                <MenuItem Header="Español" Command="{Binding ChangeLanguageCommand}" CommandParameter="fa" />
            </MenuItem>
        </Menu>
    </Grid>
</Window>
